---
python: "2.7"
sudo: required
dist: trusty

install:
  # Install Ansible.
  - pip install ansible

script:
  # Check the role/playbook's syntax.
  - ansible-playbook -i "localhost," galaxy.yml --syntax-check
  - ansible-playbook -i "localhost," -c local galaxy.yml
